@model IEnumerable<WebParaMelvin.Models.EKG>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Formulario_S_O.Id_Formulario_S_O)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Formulario_S_O.Id_Formulario_S_O)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_EKG }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_EKG }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_EKG })
        </td>
    </tr>
}

</table>
